import { GooglePlacesAutocompleteService } from '../src/google-places-autocomplete.service';


describe('First tests', () => {
  it('ts true is true test', () => {

    return expect(GooglePlacesAutocompleteService).toEqual(true);


  });
});
